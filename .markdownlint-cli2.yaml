config:
  default: true
  MD013: false      # line length: prose wraps in the renderer
  MD033: false      # inline HTML: the README banner
  MD041: false      # first line heading: the README opens with the banner
ignores:
  - node_modules
